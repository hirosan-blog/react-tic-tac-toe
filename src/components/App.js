import React,{ useState } from 'react'

const App = () => {
  const answers = [...]; 
  const answer = answers[Math.floor(Math.random()*answers.length)];

  return (

    <div>
    </div>
  )
      }
  

export default App
